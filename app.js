const PI = 3.14

// You can redeclare var, but you can not redeclare let. 
// Var is function scope, where let is block scope.

// var can be reassigned and redeclared, const can't do either of those things. 
// Const is block scope and var is function scope

// You can reassign let, but you can't reassign const.

// Hoisting is when the browser reads the declarations first and then 
// reads the values later on as it goes down through the document